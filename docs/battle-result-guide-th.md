# คู่มือระบบแสดงผลการต่อสู้ (Battle Result Popup)

## สรุปการทำงาน

ระบบจะแสดงผลการต่อสู้แบบเรียลไทม์ทันทีหลังจากการต่อสู้เสร็จสิ้น ทั้งสำหรับผู้เล่นที่กดเข้าสู้ และผู้เล่นที่รออยู่

## วิธีการทำงาน

### 📋 สำหรับผู้เล่นที่กดปุ่ม (Player 2):
1. กดปุ่ม "BATTLE" เพื่อเข้าสู่การต่อสู้
2. ส่ง Transaction ไปยัง Blockchain (จ่าย 0.01 ETH)
3. รอ Transaction เสร็จสมบูรณ์
4. ระบบจะอ่าน Event `BattleEnded` จาก Receipt
5. แสดง Popup ผลการต่อสู้ทันที

### ⏳ สำหรับผู้เล่นที่รออยู่ (Player 1):
1. ผู้เล่นอยู่ในคิวรอคู่ต่อสู้
2. เมื่อมีคนเข้ามาสู้ ระบบจะจับคู่อัตโนมัติ
3. Contract ส่ง Event `BattleEnded` ออกมา
4. Event Listener จับ Event และตรวจสอบว่าเป็นการต่อสู้ของเรา
5. แสดง Popup ผลการต่อสู้อัตโนมัติ

## การคำนวณ Combat Power (CP)

### สูตร:
```
CP = (Hunger × 30%) + (Happiness × 20%) + (Energy × 50%)
```

### ตัวอย่าง:
- Hunger = 90
- Happiness = 80  
- Energy = 100

```
CP = (90 × 0.3) + (80 × 0.2) + (100 × 0.5)
   = 27 + 16 + 50
   = 93
```

### โบนัสพิเศษ (เมื่อสถานะ ≥90):
- **Hunger ≥90**: +5% Damage (ความเสียหาย)
- **Happiness ≥90**: +5% Crit Chance (โอกาสคริติคอล)
- **Energy ≥90**: +10% Evasion (การหลบหลีก)

## หน้าจอแสดงผล

### 🎉 เมื่อชนะ:
- พื้นหลังสีเขียว-เหลือง
- ข้อความ "YOU WON!" ขนาดใหญ่
- แสดงรางวัลที่ได้รับ (0.018 ETH)
- ไอคอนเฉลิมฉลอง 🎉

### 💀 เมื่อแพ้:
- พื้นหลังสีแดง-เทา
- ข้อความ "YOU LOST..." 
- แสดงค่าเข้าสู้ที่เสียไป (0.01 ETH)
- ไอคอนกะโหลก 💀

### ข้อมูลที่แสดง:
- ที่อยู่ผู้ชนะ (Winner)
- ที่อยู่ผู้แพ้ (Loser)
- จำนวนรางวัล (Reward)
- ปุ่ม Close เพื่อปิด Modal

## ไฟล์ที่แก้ไข

### 1. Smart Contract
**`TamagotChain.sol`** - ฟังก์ชัน `calculateCP()`
```solidity
function calculateCP(address _owner) public view returns (uint256) {
    // คำนวณ CP จากสถานะจริงของสัตว์เลี้ยง
    uint256 cp = (pet.hunger * 30 / 100) + 
                 (pet.happiness * 20 / 100) + 
                 (pet.energy * 50 / 100);
    return cp;
}
```

### 2. Frontend Hook
**`useBattle.js`** - เพิ่ม State และ Event Listener
```javascript
const [battleResult, setBattleResult] = useState(null)

// อ่าน Event จาก Transaction Receipt
const parsedLog = contract.interface.parseLog(log)
if (parsedLog.name === 'BattleEnded') {
    setBattleResult({
        winner,
        loser,
        isWinner,
        reward
    })
}
```

### 3. UI Component
**`MyPet.jsx`** - เพิ่ม Battle Result Modal
```jsx
{battleResult && (
    <div className="fixed inset-0 z-[100]">
        {/* แสดง Modal ผลการต่อสู้ */}
    </div>
)}
```

## ขั้นตอนการ Deploy

### 1. Compile Contract ใหม่:
```bash
cd blockchain
npm run compile
```

### 2. Deploy ไปยัง Localhost:
```bash
npm run deploy
```

### 3. คัดลอก Contract Address:
- เปิดไฟล์ `blockchain/deployments/localhost.json`
- คัดลอก address ของ Leaderboard
- วางใน `frontend/.env`:
```
VITE_CONTRACT_ADDRESS=0x...
```

### 4. คัดลอก ABI ใหม่:
```bash
cp artifacts/contracts/Leaderboard.sol/Leaderboard.json ../frontend/src/utils/LeaderboardABI.json
```

### 5. Restart Frontend:
```bash
cd frontend
npm run dev
```

## การทดสอบ

### ✅ Test Case 1: ผู้เล่นกดเข้าสู้
1. เปิด Browser 2 หน้าต่าง
2. หน้าต่างที่ 1: กดปุ่ม BATTLE (จะเข้าคิวรอ)
3. หน้าต่างที่ 2: กดปุ่ม BATTLE (จะจับคู่ทันที)
4. **ผลลัพธ์**: หน้าต่างที่ 2 ควรเห็น Popup ผลการต่อสู้ทันที

### ✅ Test Case 2: ผู้เล่นรออยู่
1. เปิด Browser 2 หน้าต่าง
2. หน้าต่างที่ 1: กดปุ่ม BATTLE (จะเข้าคิวรอ)
3. หน้าต่างที่ 2: กดปุ่ม BATTLE (จะจับคู่ทันที)
4. **ผลลัพธ์**: หน้าต่างที่ 1 ควรเห็น Popup ผลการต่อสู้อัตโนมัติ

### ✅ Test Case 3: ตรวจสอบ CP
1. ดูค่า Stats ของสัตว์เลี้ยง (Hunger, Happiness, Energy)
2. คำนวณ CP ด้วยมือ: `(H×0.3) + (Ha×0.2) + (E×0.5)`
3. เปรียบเทียบกับค่า CP ที่แสดงในหน้า Battle
4. **ผลลัพธ์**: ค่าควรตรงกัน

### ✅ Test Case 4: ตรวจสอบรางวัล
1. บันทึก Balance ก่อนเข้าสู้
2. เข้าสู้และชนะ
3. ตรวจสอบ Balance หลังสู้
4. **ผลลัพธ์**: ควรได้รับ +0.008 ETH (0.018 - 0.01 ค่าเข้า)

## ปัญหาที่อาจพบ

### ❌ Popup ไม่แสดง
**สาเหตุ**: Event Listener ไม่ทำงาน
**แก้ไข**: 
- ตรวจสอบว่า Contract Address ถูกต้อง
- ตรวจสอบว่า ABI ใหม่ถูก copy แล้ว
- Restart Frontend

### ❌ CP ไม่ตรงกัน
**สาเหตุ**: Contract ยังใช้ค่า Fixed (50)
**แก้ไข**:
- Deploy Contract ใหม่ที่มีฟังก์ชัน `calculateCP()` ที่แก้ไขแล้ว
- อัพเดท Contract Address และ ABI

### ❌ Popup แสดงเฉพาะฝั่งเดียว
**สาเหตุ**: Event Listener ไม่ทำงานสำหรับผู้เล่นที่รอ
**แก้ไข**:
- ตรวจสอบว่า `listenToBattleEvents()` ทำงานใน useEffect
- ตรวจสอบ Console Log ว่ามี Event เข้ามาหรือไม่

## สรุป

ระบบนี้ทำให้ผู้เล่นทั้งสองฝ่ายได้รับผลการต่อสู้แบบเรียลไทม์ โดย:
- **Player 2** (ผู้กดเข้าสู้) จะเห็นผลทันทีจาก Transaction Receipt
- **Player 1** (ผู้รออยู่) จะเห็นผลอัตโนมัติจาก Event Listener
- CP คำนวณจากสถานะจริงของสัตว์เลี้ยง (30% Hunger + 20% Happiness + 50% Energy)
- UI แสดงผลสวยงามพร้อม Animation และข้อมูลครบถ้วน

---

**หมายเหตุ**: ระบบนี้ใช้ Event-driven architecture เพื่อให้ทั้งสองฝ่ายได้รับข้อมูลแบบเรียลไทม์โดยไม่ต้อง Refresh หน้าเว็บ
