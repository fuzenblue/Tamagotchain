# UI Structure Report - Tamagotchain Frontend

## หน้าเว็บที่มีในระบบ

### 1. Home Page (`/`)
- **ไฟล์**: `src/pages/Home.jsx`
- **จุดประสงค์**: หน้าแรกของเกม แสดงชื่อเกมและปุ่ม Connect Wallet
- **Components ที่ใช้**:
  - ไม่มี custom components (ใช้ HTML elements ธรรมดา)
- **Features**:
  - แสดงชื่อเกม "🐾 Tamagotchain"
  - แสดงคำขวัญ "Battle. Earn. Dominate."
  - ปุ่ม "Connect Wallet" (ยังไม่ได้เชื่อม function)

### 2. My Pet Page (`/my-pet`)
- **ไฟล์**: `src/pages/MyPet.jsx`
- **จุดประสงค์**: หน้าหลักของเกม จัดการสัตว์เลี้ยง
- **Components ที่ใช้**:
  - `PetDisplay` - แสดงภาพสัตว์เลี้ยงตามสถานะ
  - `PixelBar` - แสดงแถบสถิติ (Hunger, Happiness, Energy, Cleanliness)
  - `SideButton` - ปุ่มดำเนินการ (Feed, Play, Rest, Clean)
- **Features**:
  - แสดงสถิติสัตว์เลี้ยงแบบ real-time
  - ระบบ Stock/Cooldown สำหรับการกระทำ
  - ระบบ Buff indicators
  - Modal ยืนยันการซื้อ/ข้าม cooldown
  - ปุ่มไปหน้า Battle

### 3. Battle Arena Page (`/battle`)
- **ไฟล์**: `src/pages/Battle.jsx`
- **จุดประสงค์**: หน้าต่อสู้กับศัตรู
- **Components ที่ใช้**:
  - `PetDisplay` - แสดงสัตว์เลี้ยงของผู้เล่นและศัตรู
- **Features**:
  - แสดง Combat Power ของทั้งสองฝ่าย
  - ระบบคำนวณพลังจากสถิติ
  - Animation การต่อสู้
  - แสดงผลแพ้ชนะ
  - ปุ่ม Rematch, Home, History

### 4. Leaderboard Page (`/leaderboard`)
- **ไฟล์**: `src/pages/Leaderboard.jsx`
- **จุดประสงค์**: แสดงอันดับผู้เล่น
- **Components ที่ใช้**:
  - ไม่มี custom components (ใช้ HTML elements)
- **Features**:
  - 3 แท็บ: Global, Daily, Streak
  - แสดงอันดับและรางวัล
  - ระบบ Tier (Diamond, Gold, Silver, Bronze)
  - แสดงตำแหน่งของผู้เล่นที่ด้านล่าง

### 5. Battle History Page (`/battle-history`)
- **ไฟล์**: `src/pages/BattleHistory.jsx`
- **จุดประสงค์**: แสดงประวัติการต่อสู้
- **Components ที่ใช้**:
  - ไม่มี custom components (ใช้ HTML elements)
- **Features**:
  - ตารางแสดงประวัติการต่อสู้
  - แสดง Running Balance
  - แสดงผลแพ้ชนะและพลังที่ใช้

### 6. Profile Page (`/profile`)
- **ไฟล์**: `src/pages/Profile.jsx`
- **จุดประสงค์**: หน้าโปรไฟล์ผู้เล่น (ยังไม่ได้พัฒนา)
- **Components ที่ใช้**: ไม่มี
- **Features**: ยังไม่มี (แสดงแค่ title)

## Layout Components

### MainLayout
- **ไฟล์**: `src/components/layout/MainLayout.jsx`
- **จุดประสงค์**: Layout หลักที่ครอบทุกหน้า
- **ประกอบด้วย**: Sidebar + Main Content Area

### Sidebar
- **ไฟล์**: `src/components/common/Sidebar.jsx`
- **จุดประสงค์**: เมนูนำทางหลัก
- **Features**:
  - เมนู: Home, My Pet, Battle, History, Rank
  - แสดงสถานะ Wallet
  - Responsive (ซ่อน/แสดงในมือถือ)

## การนำทาง (Routing)
- ใช้ React Router DOM
- กำหนดใน `src/App.jsx` และ `src/router.jsx`
- ทุกหน้าใช้ MainLayout ยกเว้น Home page